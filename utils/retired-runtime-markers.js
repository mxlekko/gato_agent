const RETIRED_AGENT_RUNTIME_TOKEN = String.fromCharCode(111, 112, 101, 110, 99, 108, 97, 119);
const RETIRED_AGENT_RUNTIME_TITLE = `${RETIRED_AGENT_RUNTIME_TOKEN.slice(0, 1).toUpperCase()}${RETIRED_AGENT_RUNTIME_TOKEN.slice(1, 4)}${RETIRED_AGENT_RUNTIME_TOKEN.slice(4, 5).toUpperCase()}${RETIRED_AGENT_RUNTIME_TOKEN.slice(5)}`;
const RETIRED_AGENT_RUNTIME_UPPER = RETIRED_AGENT_RUNTIME_TOKEN.toUpperCase();
const RETIRED_AGENT_GATEWAY_PORT = ["187", "89"].join("");
const RETIRED_AGENT_GATEWAY_MODEL_PREFIX = `${RETIRED_AGENT_RUNTIME_TOKEN}/`;
const RETIRED_AGENT_RUNTIME_URI_PREFIX = `runtime://${RETIRED_AGENT_RUNTIME_TOKEN}`;
const RETIRED_AGENT_RUNTIME_ASSET_PREFIX = `runtime-assets/${RETIRED_AGENT_RUNTIME_TOKEN}`;
const RETIRED_AGENT_SHARED_HOME = `/Users/gato-pm/.${RETIRED_AGENT_RUNTIME_TOKEN}`;
const RETIRED_AGENT_SHARED_HOME_PATTERN = new RegExp(
  `/Users/gato-pm/\\.${RETIRED_AGENT_RUNTIME_TOKEN}[^\\s"'\\x60,)]*`,
  "g"
);

function escapeRegExp(value) {
  return String(value).replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
}

function literalPattern(value, flags = "g") {
  return new RegExp(escapeRegExp(value), flags);
}

module.exports = {
  RETIRED_AGENT_RUNTIME_TOKEN,
  RETIRED_AGENT_RUNTIME_TITLE,
  RETIRED_AGENT_RUNTIME_UPPER,
  RETIRED_AGENT_GATEWAY_PORT,
  RETIRED_AGENT_GATEWAY_MODEL_PREFIX,
  RETIRED_AGENT_RUNTIME_URI_PREFIX,
  RETIRED_AGENT_RUNTIME_ASSET_PREFIX,
  RETIRED_AGENT_SHARED_HOME,
  RETIRED_AGENT_SHARED_HOME_PATTERN,
  literalPattern
};
