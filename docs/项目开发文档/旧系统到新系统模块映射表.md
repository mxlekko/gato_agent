# 旧系统到新系统模块映射表

## 1. 文档目标

本文档对应阶段 7 的 `P7-T1`，用于回答一个很具体的问题：

- 当前哪些旧模块已经被新平台覆盖
- 哪些旧模块只是被新平台包装复用
- 哪些模块仍然是共享底座，暂时不应视为“已迁移完成”
- 哪些模块还完全停留在旧链路

本文档只基于当前仓库中的实际代码和已落地的并行迁移结果，不推演未来形态。

## 2. 迁移状态定义

为避免“看起来像迁移了，其实只是换了入口”的误判，本文档统一使用四种状态：

- `已覆盖`
  - 新平台已经有明确对应模块，并且该职责应以新平台实现为主
- `包装复用`
  - 新平台已把旧能力纳入统一编排，但底层仍依赖旧服务或旧实现
- `共享底座`
  - 旧新两套路径共同依赖，短期不是替换对象
- `旧链路保留`
  - 当前还没有对应的新平台实现，仍只走旧执行方式

## 3. 总体结论

截至当前版本，可以把系统分成三块：

1. 对外 API 和 scene 契约层：
   - 仍由 [routes/agent.js](/Users/gato-pm/Desktop/API_副本/routes/agent.js)、[services/scene-config.js](/Users/gato-pm/Desktop/API_副本/services/scene-config.js)、[services/request-validation.js](/Users/gato-pm/Desktop/API_副本/services/request-validation.js) 维持兼容
   - 这是 `共享底座`，不是直接删除目标

2. 面向 `sales-opportunity-advisor` / `sales-opportunity-advisor-directdb` 的业务编排层：
   - 已由 [platform/templates/grounded-structured-advisory.v1.yaml](/Users/gato-pm/Desktop/API_副本/platform/templates/grounded-structured-advisory.v1.yaml)、[platform/skills/sales-opportunity-advisor.v1.yaml](/Users/gato-pm/Desktop/API_副本/platform/skills/sales-opportunity-advisor.v1.yaml)、[platform/skills/sales-opportunity-advisor-directdb.v1.yaml](/Users/gato-pm/Desktop/API_副本/platform/skills/sales-opportunity-advisor-directdb.v1.yaml)、[platform/compiler/compile-workflow.js](/Users/gato-pm/Desktop/API_副本/platform/compiler/compile-workflow.js)、[platform/runtime/graphs/index.js](/Users/gato-pm/Desktop/API_副本/platform/runtime/graphs/index.js) 和各节点承接
   - 这是当前最主要的 `已覆盖 / 包装复用` 区域

3. 非模板化旧执行路径：
   - 包括 Gateway runtime request 构造、legacy skill 执行、direct-model 单轮调用
   - 其中 [services/runtime.js](/Users/gato-pm/Desktop/API_副本/services/runtime.js)、[services/runtime-message.js](/Users/gato-pm/Desktop/API_副本/services/runtime-message.js)、[services/response-parser.js](/Users/gato-pm/Desktop/API_副本/services/response-parser.js)、[services/direct-model.js](/Users/gato-pm/Desktop/API_副本/services/direct-model.js) 仍必须保留
   - 这部分是阶段 7 需要收口但不能立即移除的区域

## 4. 旧新模块映射表

| 旧模块 / 旧职责 | 当前新平台对应 | 迁移状态 | 仍需保留的兼容边界 | 说明 |
|---|---|---|---|---|
| [routes/agent.js](/Users/gato-pm/Desktop/API_副本/routes/agent.js) 对外 `POST /api/agent/run` 入口、HTTP envelope、顶层错误处理 | 同文件继续作为统一 API 入口，并接入 [platform/gateway/index.js](/Users/gato-pm/Desktop/API_副本/platform/gateway/index.js)、[platform/runtime/shadow.js](/Users/gato-pm/Desktop/API_副本/platform/runtime/shadow.js)、[platform/runtime/fallback.js](/Users/gato-pm/Desktop/API_副本/platform/runtime/fallback.js)、[platform/runtime/graphs/index.js](/Users/gato-pm/Desktop/API_副本/platform/runtime/graphs/index.js) | `共享底座` | 对外 URL、请求体、响应 envelope、错误码保持兼容 | 路由文件现在同时承载 `legacy / shadow / langgraph` 三种路径的总调度，但它本身仍是外部兼容壳，不是要被删除的旧模块。 |
| [services/scene-config.js](/Users/gato-pm/Desktop/API_副本/services/scene-config.js) + [scene-configs/*.json](/Users/gato-pm/Desktop/API_副本/scene-configs) 提供 scene 注册表、请求契约、旧 skill / tool / reference 描述 | 新平台通过 scene 继续读取 API 契约，同时在 `platform/` 下新增 template / skill / tool / query 配置 | `共享底座` | scene 名称、请求字段、routing.mode、旧 skill/tool/reference 元信息继续保留 | scene-config 目前还是 API 入口和兼容执行的事实来源，尚未被 `platform/skills` 完整替代。 |
| [services/request-validation.js](/Users/gato-pm/Desktop/API_副本/services/request-validation.js) 对请求参数做标准化和类型校验 | [platform/nodes/validate-input.js](/Users/gato-pm/Desktop/API_副本/platform/nodes/validate-input.js) 在图内复用相同校验逻辑 | `已覆盖` | API 层仍调用旧校验服务，图内节点调用同一能力 | 这是“能力下沉到平台节点，但实现仍复用”的典型例子。 |
| [services/runtime-message.js](/Users/gato-pm/Desktop/API_副本/services/runtime-message.js) 构造 legacy Gateway request、markers、sessionKey | [platform/nodes/legacy-scene-runner.js](/Users/gato-pm/Desktop/API_副本/platform/nodes/legacy-scene-runner.js) 统一包装 legacy 执行；新图主链不再直接依赖 runtime message | `包装复用` | fallback 和 shadow 仍要走旧 Gateway 协议 | 这部分已经从“主业务编排代码”降为“兼容执行协议适配层”。 |
| [services/runtime.js](/Users/gato-pm/Desktop/API_副本/services/runtime.js) 发起 Gateway 调用、解析 legacy scene 执行结果 | [platform/nodes/legacy-scene-runner.js](/Users/gato-pm/Desktop/API_副本/platform/nodes/legacy-scene-runner.js)、[platform/runtime/shadow.js](/Users/gato-pm/Desktop/API_副本/platform/runtime/shadow.js)、[platform/runtime/fallback.js](/Users/gato-pm/Desktop/API_副本/platform/runtime/fallback.js) | `包装复用` | 仍是 `legacy` 正式路径和 `langgraph` 自动 fallback 的目标执行器 | 阶段 7 之后它应只承担兼容和回退，不再承接新业务开发。 |
| [services/response-parser.js](/Users/gato-pm/Desktop/API_副本/services/response-parser.js) 解析 Gateway wrapped response | LangGraph 路径改由 [platform/runtime/state.js](/Users/gato-pm/Desktop/API_副本/platform/runtime/state.js) + [platform/runtime/graphs/index.js](/Users/gato-pm/Desktop/API_副本/platform/runtime/graphs/index.js) 直接产出标准结果；legacy 路径仍用旧 parser | `包装复用` | 旧 Gateway wrapped result 的解析仍不可删 | 只有 legacy / fallback 链路还需要它。 |
| `.openclaw` 中 legacy skill / agent 路由，负责真正业务编排、字段清洗、建议生成 | [platform/templates/grounded-structured-advisory.v1.yaml](/Users/gato-pm/Desktop/API_副本/platform/templates/grounded-structured-advisory.v1.yaml)、[platform/skills/README.md](/Users/gato-pm/Desktop/API_副本/platform/skills/README.md)、[platform/compiler/compile-workflow.js](/Users/gato-pm/Desktop/API_副本/platform/compiler/compile-workflow.js)、[platform/runtime/graphs/index.js](/Users/gato-pm/Desktop/API_副本/platform/runtime/graphs/index.js)、[platform/nodes](/Users/gato-pm/Desktop/API_副本/platform/nodes) | `已覆盖（销售机会模板类场景）` | 当前 prompt 资产仍有一部分来自 legacy skill 文件路径 | 对 `sales-opportunity-advisor` 和 `sales-opportunity-advisor-directdb` 来说，主流程骨架已迁入平台；但 prompt 源和 legacy 兜底路径尚未完全切断。 |
| skill 内直接读取本地字典 / 规则 / schema / prompt 资产 | [platform/nodes/load-assets.js](/Users/gato-pm/Desktop/API_副本/platform/nodes/load-assets.js) + `BusinessSkill.assetRefs` | `已覆盖` | 资产源文件本身仍大多是旧本地文件，prompt 仍可能指向 legacy skill 文件 | 资产“读取方式”已模板化，但资产“存放位置”尚未完全平台化。 |
| skill 内字段标准化、枚举映射、basis fields 选择 | [platform/nodes/normalize-facts.js](/Users/gato-pm/Desktop/API_副本/platform/nodes/normalize-facts.js) + 模板节点 `select_basis_fields` | `已覆盖` | 目前覆盖的是 grounded advisory 模板场景，不代表所有 scene 都已迁移 | 这是当前新平台覆盖最实的部分之一。 |
| skill -> ContextHelper / DirectDbRunner 获取业务上下文 | [platform/tools/sales-opportunity-context-helper.tool.yaml](/Users/gato-pm/Desktop/API_副本/platform/tools/sales-opportunity-context-helper.tool.yaml)、[platform/tools/sales-opportunity-by-opportunity-id.query.yaml](/Users/gato-pm/Desktop/API_副本/platform/tools/sales-opportunity-by-opportunity-id.query.yaml)、[platform/tools/sales-opportunity-directdb-runner.tool.yaml](/Users/gato-pm/Desktop/API_副本/platform/tools/sales-opportunity-directdb-runner.tool.yaml)、[platform/tools/sales-opportunity-directdb-by-opportunity-id.query.yaml](/Users/gato-pm/Desktop/API_副本/platform/tools/sales-opportunity-directdb-by-opportunity-id.query.yaml)、[platform/nodes/fetch-context.js](/Users/gato-pm/Desktop/API_副本/platform/nodes/fetch-context.js) | `包装复用` | [ContextHelper](/Users/gato-pm/Desktop/API_副本/ContextHelper) 和 DirectDbRunner 仍是底层后端服务 | 新平台已经把“如何选数据源、如何绑定 query profile”模板化，但数据服务本身没有下线。 |
| skill 内调用 ModelTool 做结构化输出校验 | [platform/tools/model-tool-structured-output.tool.yaml](/Users/gato-pm/Desktop/API_副本/platform/tools/model-tool-structured-output.tool.yaml) + [platform/nodes/validate-output.js](/Users/gato-pm/Desktop/API_副本/platform/nodes/validate-output.js) | `包装复用` | [ModelTool](/Users/gato-pm/Desktop/API_副本/ModelTool) 仍是真实校验后端 | 新平台已统一 tool 注册和调用协议，但 validator 服务仍沿用旧实现。 |
| skill 内 LLM 建议生成 | [platform/nodes/draft-output.js](/Users/gato-pm/Desktop/API_副本/platform/nodes/draft-output.js) + [platform/tools/openclaw-sales-agent-default.tool.yaml](/Users/gato-pm/Desktop/API_副本/platform/tools/openclaw-sales-agent-default.tool.yaml) | `部分覆盖` | 当前 V1 图内已有节点与 tool contract，但默认实现仍偏兼容草稿逻辑，尚未完全等价替代 legacy skill 的真实 agent 推理 | 这是阶段 7 以后仍需谨慎对待的缺口，不能误判为完全迁移完成。 |
| 旧日志和 requestId 级观测 | [platform/trace/context.js](/Users/gato-pm/Desktop/API_副本/platform/trace/context.js)、[platform/runtime/state.js](/Users/gato-pm/Desktop/API_副本/platform/runtime/state.js)、[platform/runtime/shadow.js](/Users/gato-pm/Desktop/API_副本/platform/runtime/shadow.js) | `已覆盖` | legacy 执行器日志和兼容态 audit 仍要保留 | 新平台已经可以记录 node runs、shadow diff、fallback audit，但旧路径原生日志仍有诊断价值。 |
| [services/direct-model.js](/Users/gato-pm/Desktop/API_副本/services/direct-model.js) 处理 `payment-info-split` 这类 direct-model 场景 | 当前 `platform/` 下无对应 template / skill / node 组合；仅由 [platform/gateway/index.js](/Users/gato-pm/Desktop/API_副本/platform/gateway/index.js) 在路由策略层识别其只能 `legacy` | `旧链路保留` | `payment-info-split` 仍完全依赖旧 direct-model 实现 | 这是当前最明确的“还没有迁移到新平台”的模块组。 |

## 5. Scene 维度的迁移归属

| Scene | 当前归属 | 说明 |
|---|---|---|
| `sales-opportunity-advisor` | `并行双轨，模板已建立` | 已具备 template / business skill / tool / query / graph；scene routing 允许 `legacy / shadow / langgraph`，但默认仍是 `legacy`。 |
| `sales-opportunity-advisor-directdb` | `模板已建立，但对外仍只开 legacy` | 第二业务配置已落地并可编译通过，stub e2e 已验证；当前 scene-config 仍只允许 `legacy`，尚未打开 shadow / langgraph。 |
| `payment-info-split` | `旧链路保留` | 仍由 [services/direct-model.js](/Users/gato-pm/Desktop/API_副本/services/direct-model.js) 直接执行，没有对应模板化 workflow。 |

## 6. 现阶段必须保留的兼容边界

阶段 7 之前，下面这些边界不能提前删：

1. [routes/agent.js](/Users/gato-pm/Desktop/API_副本/routes/agent.js)
   - 继续作为唯一对外 API 入口
2. [scene-configs](/Users/gato-pm/Desktop/API_副本/scene-configs)
   - 继续作为 scene 级请求契约、routing 开关和 legacy 元信息来源
3. [services/runtime.js](/Users/gato-pm/Desktop/API_副本/services/runtime.js) + [services/runtime-message.js](/Users/gato-pm/Desktop/API_副本/services/runtime-message.js) + [services/response-parser.js](/Users/gato-pm/Desktop/API_副本/services/response-parser.js)
   - 继续作为 legacy / fallback 执行路径
4. [ContextHelper](/Users/gato-pm/Desktop/API_副本/ContextHelper) 与 [ModelTool](/Users/gato-pm/Desktop/API_副本/ModelTool)
   - 继续作为新平台注册 tool 背后的真实服务
5. [services/direct-model.js](/Users/gato-pm/Desktop/API_副本/services/direct-model.js)
   - 继续承接尚未模板化的 direct-model 场景
6. `.openclaw` 旧 skill 文件
   - 当前至少仍承担部分 prompt / 兼容执行资产来源

## 7. 给 `P7-T2` 的直接结论

从当前映射关系看，下一步不能做“删除旧 runtime”，而应该做的是：

- 把 [services/runtime.js](/Users/gato-pm/Desktop/API_副本/services/runtime.js) 一类模块明确降级为兼容执行器
- 把新业务默认入口固定到 `platform/templates + platform/skills + platform/tools + platform/compiler + platform/runtime`
- 明确 `payment-info-split` 这类 direct-model scene 目前不在本轮收口范围

也就是说，阶段 7 的正确方向是“旧编排降级为兼容层”，不是“提前清理所有旧模块”。
