# 复制为 .env.local 后可覆盖本机控制台代理目标。
# 默认 3200 控制台应代理到当前 _副本 API： http://127.0.0.1:3100
# 如果后续启用独立实验端口，可只改下面这一行，然后重启 `npm run dev`。
VITE_API_PROXY_TARGET=http://127.0.0.1:3100

# 可选：复制到 .env.local 后可填本机控制台管理员 token。
# VITE_CONSOLE_ADMIN_TOKEN=
