# 控制台 UI 直接请求后端时可填绝对地址；留空则走 Vite /api 代理。
VITE_API_BASE_URL=
# 默认代理到当前 _副本 API（http://127.0.0.1:3100）。
# 如需切到其他实验 API 端口，请在本地新建 .env.local 并覆盖该值。
VITE_API_PROXY_TARGET=http://127.0.0.1:3100

# 可选：仅内网/本地调试时让前端自动带控制台管理员 token；公网不要把管理员 token 打进前端包。
# VITE_CONSOLE_ADMIN_TOKEN=
